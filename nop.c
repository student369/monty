#include "monty.h"
/**
 * nop - that not do nothing.
 * @stack: the stack
 * @line_number: The line number
 *
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
